version: "2"
sql:
  - engine: "postgresql"
    queries: "./database/queries/"
    schema: "./database/migrations/schema.sql"
    gen:
        go:
           emit_json_tags: true
           emit_interface: true
           emit_empty_slices: true
           out: "./gen"